initContainers:
  enabled: true
schema:
  enabled: true
  db_type: mysql
component: shipping
appVersion: 1.7
appName: shipping

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

  ingress:
    enabled: true
    host: shipping-dev.pdevops78.online