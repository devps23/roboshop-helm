initContainers:
  enabled: true

schema:
  enabled: true
  db_type: mysql
  schemaVersion: 1.3

component: shipping
appVersion: 2.2
appName: shipping

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

  ingress:
    enabled: false
