initContainers:
  enabled: true

schema:
  enabled: true
  db_type: mysql

component: shipping
appVersion: 1.3
appName: shipping

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

  ingress:
    enabled: false
